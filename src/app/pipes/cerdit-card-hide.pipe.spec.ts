import { CerditCardHidePipe } from './cerdit-card-hide.pipe';

describe('CerditCardHidePipe', () => {
  it('create an instance', () => {
    const pipe = new CerditCardHidePipe();
    expect(pipe).toBeTruthy();
  });
});
